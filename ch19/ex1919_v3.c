#include <stdio.h>

int main(void) {
   char *fruit[] = {"watermelon", "banana", "pear",     "apple",
                    "coconut",    "grape",  "blueberry"};
   char **p = fruit;
   int i, x;

   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   putchar('\n');
   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

   for (x = 0; x < 7; x++) {
      printf("%d\t", x);
      printf("(fruit+%d) -> %p\t", x, (fruit + x));
      printf("fruit[%d] -> %p\t", x, fruit[x]);
      printf("*fruit[%d] -> %c\n", x, *fruit[x]);
   }

   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   putchar('\n');
   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

   for (x = 0; x < sizeof(fruit) / sizeof(char **); x++) {
      while (putchar(*(*(fruit + x))++)) {
         ;
      }
      putchar('\n');
   }

   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   putchar('\n');
   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

   for (x = 0; x < 7; x++) {
      printf("%d\t", x);
      printf("(fruit+%d) -> %p\t", x, (fruit + x));
      printf("fruit[%d] -> %p\t", x, fruit[x]);
      printf("*fruit[%d] -> %c\n", x, *fruit[x]);
   }

   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   putchar('\n');
   // ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

   return 0;
}
